---
phase: 07-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/client.ts
  - src/app/page.tsx
  - .planning/phases/05-progress-dashboard/05-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [PROG-01, PROG-02, PROG-03]

must_haves:
  truths:
    - "Phase 5 has a VERIFICATION.md confirming PROG-01, PROG-02, PROG-03 are satisfied"
    - "createClerkSupabaseClient dead code no longer exists in the codebase"
    - "Stale getLessonProgressForScope import no longer exists in src/app/page.tsx"
    - "npx tsc --noEmit compiles cleanly after all changes"
  artifacts:
    - path: ".planning/phases/05-progress-dashboard/05-VERIFICATION.md"
      provides: "Phase 5 verification report closing PROG-01, PROG-02, PROG-03 audit gaps"
      min_lines: 100
    - path: "src/app/page.tsx"
      provides: "Dashboard page with stale import removed"
      contains: "import { getContinueLesson } from"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "src/lib/progress.ts, src/lib/actions/progress.ts, src/app/page.tsx, src/app/pillars/*/page.tsx"
      via: "evidence citations referencing actual code paths"
      pattern: "markLessonInProgress|markLessonComplete|getContinueLesson|isSemesterLocked|getLessonProgressForScope"
    - from: "src/app/page.tsx"
      to: "src/lib/progress.ts"
      via: "import of getContinueLesson (must remain intact after stale import removal)"
      pattern: "import.*getContinueLesson.*from.*@/lib/progress"
---

<objective>
Close all verification and code quality gaps identified in the v1.0 milestone audit: remove dead code (`createClerkSupabaseClient`), fix stale import (`getLessonProgressForScope` in dashboard), and write the missing Phase 5 VERIFICATION.md that documents PROG-01, PROG-02, PROG-03 as satisfied.

Purpose: The v1.0 audit found Phase 5 has no VERIFICATION.md (breaking the verification chain) and two code hygiene issues. All three PROG requirements are implemented and working — this is a documentation and cleanup gap, not an implementation gap. Closing it makes the audit return "satisfied" for all Phase 5 requirements.

Output: Clean codebase (dead code removed, stale import fixed) and a thorough 05-VERIFICATION.md following the project's established verification format.
</objective>

<execution_context>
@/Users/benjaminbarwo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benjaminbarwo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-verification-cleanup/07-RESEARCH.md
@.planning/phases/05-progress-dashboard/05-01-SUMMARY.md
@.planning/phases/05-progress-dashboard/05-02-SUMMARY.md

# Format reference for VERIFICATION.md structure
@.planning/phases/04-quiz-engine/04-VERIFICATION.md

# Source files to read for verification evidence (Task 2)
@src/lib/progress.ts
@src/lib/actions/progress.ts
@src/components/lesson/MarkCompleteButton.tsx
@src/app/page.tsx
@src/app/pillars/[pillarSlug]/page.tsx
@src/app/pillars/[pillarSlug]/semesters/[semesterSlug]/page.tsx
@src/app/pillars/[pillarSlug]/semesters/[semesterSlug]/courses/[courseSlug]/page.tsx

# Dead code file to delete (Task 1)
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and fix stale import</name>
  <files>src/lib/supabase/client.ts, src/app/page.tsx</files>
  <action>
Three changes in sequence:

1. **Grep safety check**: Run `grep -r "supabase/client" src/ --include="*.ts" --include="*.tsx"` to confirm zero imports of `@/lib/supabase/client` from any file other than itself. If ANY external imports are found, STOP and document them — do not delete the file.

2. **Delete dead code file**: Delete `src/lib/supabase/client.ts` entirely. This file exports only `createClerkSupabaseClient` which was superseded by the server actions pattern (`src/lib/actions/progress.ts`) in Phase 5. The function is never imported by any other file. Deleting removes the dead `useSession` import from `@clerk/nextjs` as well.

3. **Fix stale import in dashboard**: In `src/app/page.tsx`, change the import line:
   ```typescript
   // BEFORE:
   import { getContinueLesson, getLessonProgressForScope } from '@/lib/progress'
   // AFTER:
   import { getContinueLesson } from '@/lib/progress'
   ```
   `getLessonProgressForScope` is imported but never called in this file — the dashboard computes pillar progress via in-memory Set intersection instead. `getContinueLesson` IS used and must remain.

4. **Verify clean compile**: Run `npx tsc --noEmit` in the project root. It must complete with zero errors. If it fails, diagnose and fix before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/benjaminbarwo/Downloads/Learning && grep -r "supabase/client" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" ; test ! -f src/lib/supabase/client.ts && echo "DELETED OK" ; grep "getLessonProgressForScope" src/app/page.tsx ; echo "Exit: $?" ; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/lib/supabase/client.ts` does not exist (deleted)
    - No file in `src/` imports from `@/lib/supabase/client`
    - `src/app/page.tsx` imports only `getContinueLesson` from `@/lib/progress` (no `getLessonProgressForScope`)
    - `npx tsc --noEmit` passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 5 VERIFICATION.md from code evidence</name>
  <files>.planning/phases/05-progress-dashboard/05-VERIFICATION.md</files>
  <action>
Create `.planning/phases/05-progress-dashboard/05-VERIFICATION.md` following the exact format established by `04-VERIFICATION.md`. This is the most important deliverable of Phase 7 — it must verify what was BUILT, not what was PLANNED. The key architectural deviation: Phase 5 used server actions with `createAdminSupabaseClient` (not `createClerkSupabaseClient`) for all progress mutations.

**CRITICAL: Read every source file listed in the context section BEFORE writing any verification claims.** Every evidence citation must reference actual file paths and describe real code, not plan intentions.

**Structure (follow 04-VERIFICATION.md format exactly):**

1. **Frontmatter:**
   - `phase: 05-progress-dashboard`
   - `verified: 2026-03-01T12:00:00Z`
   - `status: passed` (all truths verified by code reading + Phase 5 summaries confirm human verification of Task 2 checkpoint was approved)
   - `score: 5/5 must-haves verified`
   - `re_verification: true` (this is gap closure re-verification, not initial)
   - `human_verification:` array with items that require runtime/visual confirmation (similar to Phase 4's format)

2. **Observable Truths table (5 rows):**

   | # | Truth | Status | Evidence |
   |---|-------|--------|---------|
   | 1 | Lesson status (not_started/in_progress/completed) updates and persists | VERIFIED | `markLessonInProgress` in `src/lib/progress.ts` called from lesson `page.tsx` on every render; `markLessonComplete` server action in `src/lib/actions/progress.ts` called by `MarkCompleteButton.tsx`; progress upsert to `progress` table with `on_conflict: 'user_id,lesson_id'` |
   | 2 | Dashboard shows "continue" card pointing to most recently accessed incomplete lesson | VERIFIED | `getContinueLesson` in `src/lib/progress.ts` called from `src/app/page.tsx`; returns lesson with full URL path (pillar/semester/course/lesson slugs joined); dashboard conditionally renders continue card vs recommended start CTA |
   | 3 | Progress bars reflect real completion % at pillar and course levels | VERIFIED | Dashboard: batched queries + Set intersection for per-pillar percent; Pillar page: `pillarPercent` computed from all pillar lessons; Semester page: per-course progress bars; Course page: `getLessonProgressForScope` + `ProgressBar` |
   | 4 | Semester N+1 locked until Semester N completed; manual override works | VERIFIED | `isSemesterLocked()` pure function in `progress.ts`; enforced at pillar page (non-clickable div with padlock SVG) and semester page (locked UI card with back-link); `manually_unlocked` boolean column on semesters table (migration `00003_semester_unlock.sql`) |
   | 5 | MarkCompleteButton shows loading state and error feedback | VERIFIED | `MarkCompleteButton.tsx`: `isLoading` state disables button + shows spinner SVG; `setErrorMessage` on catch; `<p role="alert">` displays error; `router.refresh()` triggers RSC re-render |

   **Evidence must cite actual file paths and describe real implementations.** Read each source file to extract evidence — do NOT copy from the plan or summaries.

3. **Required Artifacts table:**
   List each key file with its expected role and verification status:
   - `src/lib/progress.ts` — 5 exported helpers (getLessonProgressForScope, getContinueLesson, markLessonInProgress, isSemesterLocked, getLessonStatuses)
   - `src/lib/actions/progress.ts` — Server action markLessonComplete using createAdminSupabaseClient
   - `src/components/lesson/MarkCompleteButton.tsx` — Client component calling markLessonComplete server action
   - `src/app/page.tsx` — Dashboard with continue card + batched pillar progress
   - `src/app/pillars/[pillarSlug]/page.tsx` — Pillar progress bar + semester lock indicators
   - `src/app/pillars/[pillarSlug]/semesters/[semesterSlug]/page.tsx` — Semester lock enforcement + per-course progress bars
   - `src/app/pillars/[pillarSlug]/semesters/[semesterSlug]/courses/[courseSlug]/page.tsx` — LessonStatusIcon + course progress bar
   - `supabase/migrations/00003_semester_unlock.sql` — ALTER TABLE adding manually_unlocked column

4. **Key Link Verification table:**
   Verify these connections:
   - lesson page.tsx -> markLessonInProgress (fire-and-forget on render)
   - MarkCompleteButton -> markLessonComplete server action -> adminSupabase upsert
   - dashboard page.tsx -> getContinueLesson -> progress table query
   - pillar page.tsx -> isSemesterLocked -> visual lock rendering
   - semester page.tsx -> isSemesterLocked -> page-level lock enforcement
   - course page.tsx -> getLessonStatuses + getLessonProgressForScope (parallel via Promise.all)

5. **Requirements Coverage table:**
   | PROG-01 | SATISFIED | Evidence citing actual mutation + query paths |
   | PROG-02 | SATISFIED | Evidence citing all 4 hierarchy pages with real progress data |
   | PROG-03 | SATISFIED | Evidence citing isSemesterLocked enforcement points + manually_unlocked |

   Note: All progress reads and writes use `createAdminSupabaseClient()` which bypasses RLS. This is the intentional single-user architecture (documented in State decisions). RLS enforcement requires Clerk sign-in UI (AUTH features in v2). This follows the same pattern as Phase 4's QUIZ-03 "SATISFIED (with auth gap)" documentation.

6. **Anti-Patterns Found:** Scan for stub code, placeholder values, TODOs in the Phase 5 source files. Document any found (expected: none — Phase 5 replaced all Phase 2 mock values with real data).

7. **Human Verification section:** List items that require a running browser to confirm:
   - Visual verification of progress bars rendering correctly
   - Continue card pointing to correct lesson
   - Locked semester UI rendering (padlock icon, back-link)
   - Mark complete button state transitions (loading spinner, error message, completion state)

8. **Gaps Summary:** Expected: no gaps. All truths verified, all artifacts substantive, all key links wired, all requirements satisfied.
  </action>
  <verify>
    <automated>test -f /Users/benjaminbarwo/Downloads/Learning/.planning/phases/05-progress-dashboard/05-VERIFICATION.md && wc -l /Users/benjaminbarwo/Downloads/Learning/.planning/phases/05-progress-dashboard/05-VERIFICATION.md | awk '{if ($1 >= 100) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>
    - `05-VERIFICATION.md` exists in `.planning/phases/05-progress-dashboard/`
    - Document follows the exact format of `04-VERIFICATION.md` (frontmatter, Observable Truths, Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns, Human Verification, Gaps Summary)
    - All 3 requirements (PROG-01, PROG-02, PROG-03) are marked SATISFIED with evidence from actual source files
    - Evidence cites real code paths (server actions, adminSupabase, not createClerkSupabaseClient)
    - Document is at least 100 lines (thoroughness check)
  </done>
</task>

</tasks>

<verification>
Phase 7 is complete when ALL of the following are true:

1. `src/lib/supabase/client.ts` does not exist — dead code removed
2. `grep -r "getLessonProgressForScope" src/app/page.tsx` returns zero matches — stale import removed
3. `npx tsc --noEmit` passes with zero errors — codebase compiles cleanly
4. `.planning/phases/05-progress-dashboard/05-VERIFICATION.md` exists with 100+ lines
5. 05-VERIFICATION.md contains "PROG-01", "PROG-02", "PROG-03" each marked as "SATISFIED"
6. 05-VERIFICATION.md references `markLessonComplete` server action (not `createClerkSupabaseClient`) as the mutation mechanism — verifying actual code, not planned code
</verification>

<success_criteria>
- Phase 5 verification chain is closed: 05-VERIFICATION.md exists with all 3 PROG requirements documented as satisfied
- Dead code removed: createClerkSupabaseClient file deleted, zero import references remain
- Stale import fixed: getLessonProgressForScope removed from dashboard import, getContinueLesson retained
- TypeScript compiles cleanly: npx tsc --noEmit passes
- A re-run of the v1.0 milestone audit would return PROG-01, PROG-02, PROG-03 as "satisfied" instead of "partial"/"unsatisfied"
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-cleanup/07-01-SUMMARY.md`
</output>
